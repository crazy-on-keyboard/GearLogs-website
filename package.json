{
  "name": "gearlogs-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-site.mjs && vite preview",
    "build": "node scripts/build-site.mjs",
    "build:en": "node scripts/build-site.mjs --en-only",
    "preview": "vite preview",
    "check:links": "node scripts/check-links.mjs",
    "check:i18n": "node scripts/check-i18n.mjs",
    "check:types": "tsc -p .",
    "check:contact": "node scripts/check-contact.mjs",
    "check": "npm run check:types && npm run check:contact && npm run build && npm run check:links && npm run check:i18n"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "vite": "^6.2.0"
  }
}
