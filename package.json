{
  "name": "gearlogs-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:links": "node scripts/check-links.mjs",
    "check": "npm run build && npm run check:links"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
